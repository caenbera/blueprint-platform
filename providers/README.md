# Providers

Contexto global de React: sesión/usuario autenticado, organización activa, tema (light/dark), y en el futuro el estado del Navigator Engine y Mission Control Engine si se decide exponerlos vía Context en lugar de solo hooks (Prompt 13, "Estado Global").
