# Types

Tipos de TypeScript compartidos, en particular las entidades del dominio (`Organization`, `Project`, `Blueprint`, `Phase`, `Module`, `Chapter`, `Workspace`, `Card`, `KnowledgeItem`, `Document`, `MarketplaceResource`) descritas en `docs/blueprint-master-spec.md`. Estos tipos son el contrato entre `services/` y el resto de la app.
