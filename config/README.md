# Config

Constantes de configuración de la app: nombres de colecciones de Firestore, catálogo de tipos de Card, catálogo de roles/permisos, límites de paginación, feature flags. Nada de secretos aquí — eso vive en variables de entorno (`.env.local`).
