# Hooks

Lógica de estado y efectos reutilizable (React hooks). Aquí vive la lógica de dominio que los Feature Components consumen — nunca al revés.

Convención: `use<Dominio><Accion>.ts`, p. ej. `useWorkspaceAutosave.ts`, `useNavigatorTree.ts`.
